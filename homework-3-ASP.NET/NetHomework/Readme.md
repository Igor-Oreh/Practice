Домашнее задание по .Net
Для запуска проекта введите в консоль:
dotnet build --configuration Release
dotnet run

В данном домашнем задании выполнены вырианты:
7.   Работа с телом запроса (JSON)
Создайте модель User (с полями Name и Age) и действие POST в контроллере, которое принимает объект User из тела запроса и возвращает его обратно с сообщением "Пользователь получен".

Сделал простой контроллер, принимающий и воззвращающий User


12.   Манипуляция ответом
Создайте middleware, который добавляет кастомный заголовок X-Response-Time с текущей датой и временем в каждый исходящий ответ.

Создал, время решил возвращать в стандартном формате, так как в условии формат не указан, хоть и слышал про RFC